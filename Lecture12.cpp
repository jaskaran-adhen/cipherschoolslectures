
#include <iostream>
using namespace std;

int main() {
    int num;
    int sum = 0;

    do {
        cout << "Enter";
        cin >> num;

        if (num > 0) {
            sum += num;
        }

    } while (number >= 0);

    cout << " total" << sum << endl;

    return 0;
}
